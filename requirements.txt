# Runtime dependencies.
#
# Upper bounds are on major versions, not exact pins: an exact pin rots (the
# wheel stops building on new Pythons), while an unbounded `>=` lets a future
# breaking release silently change the reported numbers. The exact versions the
# reported results were produced with are recorded in README.md under
# "Reproducing the reported results" -- regenerate that list with
# `pip freeze > requirements-lock.txt` if you need byte-identical output.

pandas>=2.0,<4.0          # every module
numpy>=1.24,<3.0          # every module
scikit-learn>=1.3,<2.0    # the modeling layer: RidgeCV, StandardScaler,
                          # TransformedTargetRegressor, LedoitWolf, metrics
nba_api>=1.11.4,<2.0      # NBA Stats API client -- pull_*.py only; every
                          # modeling script runs from the committed CSVs

# Only needed by the two standalone experiments (see README):
scipy>=1.10,<2.0          # scipy.stats.t, for the across-fold CIs in
                          # lineup_defense_model.py (also a scikit-learn dep,
                          # so listed for clarity rather than necessity)
matplotlib>=3.7,<4.0      # the GP/MIN histograms in pull_player_stats.py that
                          # MIN_GP / MIN_MPG were chosen from